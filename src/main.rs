use rust_book_problems::{
    // vending_machine, 
    // guess_number, 
    // first_word, 
    // build_user, 
    // rectangle,
    //minigrep,
    closure_practice,
};

fn main() {
    //guess_number::guess_number();

    //first_word::return_first_word();

    // let mut user = build_user::build_user();
    // println!("User created: {}, {}", user.username.trim(), user.email.trim());

    // let rect1 = rectangle::Rectangle::new( 30, 50 );
    // let rect2 = rectangle::Rectangle::new( 10, 60 );
    // println!("The area of the rectangle is {} square pixels.", rect1.area());
    // println!("Can rect1 hold rect2? {}", rect1.can_hold(&rect2));

    //vending_machine::get_product()

    //minigrep::minigrep()

    closure_practice::run()
}